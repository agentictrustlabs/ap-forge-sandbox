# ap-forge-sandbox

The forge the agenticprimitives review-loop gate works on. Every PR here was opened, reviewed and promoted by an agent under a mandate; the PR body names the intent digest and the run.
